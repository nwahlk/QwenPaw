# 13 — 测试策略与质量门禁（引导版模板）

---

| 项 | 值 |
|---|---|
| 模块编号 | M1-QA |
| 模块名称 | 投研问答助手 |
| 文档版本 | v0.1 |
| 阶段 | Test（断言 — 怎样算通过） |
| 追溯文档 | `05` UserStory · `09` API 接口规格 · `14` 追踪矩阵 |

---

## 一、测试分层

> 共 4 层，写测试前先搞清楚每层验什么。

| 层级 | 工具 | 覆盖对象 | 说明 |
|------|------|----------|------|
| **L1 Unit** | pytest | 单个函数/类的逻辑 | 不依赖网络，最快 |
| **L2 Integration** | pytest + Flask test_client | API 端点 + 存储 | 模拟 HTTP 请求 |
| **L3 Contract** | pytest + schema 断言 | 响应体结构与 `09` 一致 | 验字段名、类型 |
| **L4 E2E** | Playwright / Cypress | 完整用户旅程 | 浏览器级操作 |

## 二、用例拆分原则

> **核心理念：数"可独立失败的点"** — 每条 TC 只回答一个问题，失败时不看代码就知道哪里坏了。

| 来源 | 计数方法 | 示例 |
|------|----------|------|
| 正常路径 | 每个端点的 happy path = 1 条 | `POST /ask → 200` = TC-001 |
| 错误码 | 每个 distinct error code = 1 条 | `EMPTY_QUERY` = TC-002 |
| 分支路径 | 每个 if/else 降级分支 = 1 条 | 无 API Key → demo = TC-004 |
| 类型约束 | Spec `09` 约定的字段类型 = 1 条 | `llm_used` 必须 bool = TC-006 |

## 三、★ 示例：已有测试用例

| TC-ID | 层级 | 测试函数 | 断言要点 | 关联 REQ |
|-------|------|----------|----------|----------|
| TC-M01-001 | Integration | `test_ask_success` | `POST /ask` → 200, 含 answer、llm_used、traceId | REQ-002 |
| TC-M01-002 | Integration | `test_ask_empty_query` | 空 query → 400, `EMPTY_QUERY` | REQ-002 |
| TC-M01-003 | Integration | `test_ask_long_query` | query >500 → 400, `INVALID_QUERY` | REQ-002 |
| TC-M01-004 | Integration | `test_bailian_fallback` | 无 API Key → `answer_source='demo'` | REQ-002 |

### 3.1 会话管理 API 测试（请填写）

> 覆盖 GET /sessions、POST /sessions、DELETE /sessions/<id>。至少写 3~4 条。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-020 | L2 Integration | `GET /sessions` → 200，返回 `sessions` 数组，每个元素含 id、title、created_at、query_count | REQ-001 | P0 |
| TC-M01-021 | L2 Integration | `POST /sessions` → 201，返回 session_id、title、created_at、query_count=0、traceId | REQ-001 | P0 |
| TC-M01-022 | L2 Integration | `POST /sessions` 未传 title → 201，title 默认为"新会话" | REQ-001 | P0 |
| TC-M01-023 | L2 Integration | `DELETE /sessions/<id>` → 200，会话与关联记录均被删除（级联删除） | REQ-001 | P0 |
| TC-M01-024 | L2 Integration | `DELETE /sessions/<id>` 不存在的 session_id → 404，`SESSION_NOT_FOUND` | REQ-001 | P1 |
| TC-M01-025 | L3 Contract | `GET /sessions` 响应体字段类型与 `09` §5 一致（sessions[] 内每个对象的字段名与类型） | REQ-001 | P0 |

### 3.2 问答记录 API 测试（请填写）

> 覆盖 GET /sessions/<id>/records。有记录和无记录两种场景。

| TC-ID | 层级 | 断言要点 | 关联 REQ | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-030 | L2 Integration | `GET /sessions/<id>/records` 有记录 → 200，返回 records 数组，每条含 query、answer、llm_used、answer_source、timestamp，按时间正序 | REQ-003 | P0 |
| TC-M01-031 | L2 Integration | `GET /sessions/<id>/records` 无记录 → 200，返回空数组 `[]` | REQ-003 | P1 |
| TC-M01-032 | L3 Contract | `GET /capabilities` → 200，含 copaw_configured、bailian_configured、model 字段，类型与 `09` §1 一致 | REQ-005 | P0 |
| TC-M01-033 | L2 Integration | `GET /health` → 200，含 status、uptime、各组件状态；数据目录可写时 status=healthy | REQ-004 | P0 |
| TC-M01-034 | L2 Integration | `GET /health` 数据目录不可写时 → status=degraded | REQ-004 | P1 |

### 3.3 Storage 层单元测试（请填写）

> 覆盖 Storage 类核心方法：`__init__`、`create_session`、`get_sessions`、`delete_session`（级联）、`add_record`、`get_records_by_session`。

| TC-ID | 层级 | 覆盖方法 | 断言要点 | 优先级 |
|-------|------|----------|----------|--------|
| TC-M01-040 | L1 Unit | `Storage.__init__` | 数据目录不存在时自动创建，sessions.json 与 qa_records.json 初始化为空数组 | P0 |
| TC-M01-041 | L1 Unit | `create_session` | 传入 session_id + title → 返回 dict 含所有字段，query_count=0，sessions.json 新增一条 | P0 |
| TC-M01-042 | L1 Unit | `get_sessions` | 返回全部会话列表，按 created_at 倒序 | P0 |
| TC-M01-043 | L1 Unit | `delete_session` | 删除会话后 sessions.json 不包含该 session_id，关联 qa_records.json 记录同步清除（级联删除） | P0 |
| TC-M01-044 | L1 Unit | `add_record` | 写入记录后 qa_records.json 新增一条，对应 session 的 query_count +1，updated_at 更新 | P0 |
| TC-M01-045 | L1 Unit | `get_records_by_session` | 按 session_id 过滤，返回该会话全部记录，按 timestamp 正序 | P0 |
| TC-M01-046 | L1 Unit | `update_session` | 传入 session_id + title → 返回更新后 dict，title 已变更，updated_at 已更新 | P1 |
| TC-M01-047 | L1 Unit | `delete_records_by_session` | 删除指定 session_id 下所有记录，返回删除条数，其他 session 记录不受影响 | P0 |
| TC-M01-048 | L1 Unit | `add_record`（首次问答） | query_count 从 0→1 时，session title 自动重命名为 query 前 20 字 + "..." | P0 |
| TC-M01-049 | L1 Unit | `add_record`（校验） | query 为空或超 500 字符时抛出 ValueError，不写入存储 | P0 |

## 四、质量门禁

| Gate-ID | 条件 | 阻塞合并 |
|---------|------|----------|
| **G-LINT** | `black --check` + `flake8` 零错误 | **是** |
| **G-UNIT** | L1 Unit 测试全绿，覆盖率 ≥ **85%** | **是** |
| **G-INT** | L2/L3 Integration + Contract 测试全绿 | **是** |
| **G-AC** | P0 验收标准 100% TC 覆盖，P1 覆盖 ≥ **80%** | **是** |
| **G-PERF** | `response_time_ms` P95 < **5000ms**；会话管理端点 < **500ms** | 否（warning） |

### 执行命令

```bash
# 后端测试
cd groups/Group-X/NN/backend
python -m pytest tests/ -q

# 后端测试 + 覆盖率
python -m pytest tests/ -q --cov=. --cov-report=term-missing
```

## 五、TDD 适用场景

| 场景 | TDD 价值 | 建议 |
|------|----------|------|
| 简单 CRUD（列表、删除） | 低 | 写完补测试 |
| 参数校验 / 错误码 | 中 | 建议 TDD |
| 多级降级 / 状态机 | **高** | **强烈建议 TDD** |
| 金融计算 / 风险判定 | **极高** | **必须 TDD** |

## 六、追溯关系

> 汇总所有 TC 与 REQ 的映射，完成后同步到 `14` 追踪矩阵。

| 需求编号 | 已实现 TC | 待补充 TC | 关联 API 端点 |
|----------|-----------|-----------|----------------|
| REQ-001 (US-001 会话管理) | — | TC-M01-020～025、TC-M01-041～043 | `POST /sessions`、`GET /sessions`、`DELETE /sessions/<id>` |
| REQ-002 (US-002 问答提交) | TC-M01-001～004 | TC-M01-044、TC-M01-048、TC-M01-049 | `POST /ask` |
| REQ-003 (US-003 历史记录) | — | TC-M01-030、TC-M01-031、TC-M01-045 | `GET /sessions/<id>/records` |
| REQ-004 (US-004 健康检查) | — | TC-M01-033、TC-M01-034 | `GET /health` |
| REQ-005 (US-005 能力探测) | — | TC-M01-032 | `GET /capabilities` |

---

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-04-14 | 首版填写 |
